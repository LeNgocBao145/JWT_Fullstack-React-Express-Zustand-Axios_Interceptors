export type User = {
    _id: string;
    displayName: string;
    username: string;
    email: string;
    avatarUrl?: string,
    bio?: string;
    phone?: string;
    createAt?: string;
    updatedAt?: string;
}